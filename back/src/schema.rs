table! {
    data (id) {
        id -> Int8,
        title -> Varchar,
        description -> Varchar,
        date -> Date,
        priority -> Varchar,
        duration -> Int4,
        status -> Varchar,
    }
}
